{
  "name": "mariadb-test",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "jest",
    "dcps": "docker-compose -f docker/docker-compose.yml ps",
    "dcup": "docker-compose -f docker/docker-compose.yml up -d",
    "dcdown": "docker-compose -f docker/docker-compose.yml down"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Kyome22/mariadb-test.git"
  },
  "author": "kyome",
  "homepage": "https://github.com/Kyome22/mariadb-test#readme",
  "dependencies": {
    "dotenv": "^8.2.0",
    "mariadb": "^2.5.3"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
